source :rubygems

gem 'i18n'
gem 'activesupport', :require => 'active_support/core_ext'
gem 'eventmachine'
gem 'em-http-request'
gem 'happening'
gem 'trollop'
gem 'releasy'

if ENV['OS'] == 'Windows_NT'
  gem 'win32console'
  gem 'term-ansicolor'
else
  gem 'colorize'
end